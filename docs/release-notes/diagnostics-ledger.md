# Release diagnostics ledger

Maintainer file, not user docs. Every stable or RC release adds one log line per change that field testers are asked to confirm, and removes the previous release's lines once their issues are confirmed or closed; unconfirmed lines are carried forward under the new version. Complicated fixes add their line in the fix commit (AGENTS.md "Diagnostic log rides complicated fixes"); simple UI fixes need none; the release pass (`publish-release`, `publish-rc-release`) adds the rest and does the trim.

Convention: a release-specific line carries `extra.releaseCheck = "<version>/<slug>"` so it can be found with `git grep -n releaseCheck` and trimmed by version. General diagnostics (the sync trail, `[sync-encryption]` events, `Mobile background sync started`/`finished`, activation proofs) never carry `releaseCheck` and are never trimmed.

## General (keep)

- Sync trail: `Sync start`, `Sync step`, `Sync read check found no changes`, `Sync diagnostic complete` (core `sync-run.ts`, both apps). `Sync cycle requeued` carries `reason` and, since #1170, `detail` = the transport's own message (for a WebDAV 412: `WEBDAV_REMOTE_WRITE_CONFLICT: WebDAV document changed before replacement (412)`), so a server rejecting its own ETag shows in one line.
- `[sync-encryption]` state / remote-read / transition / error / activation (core + both apps).
- `Mobile background sync started` / `finished` / `run took longer than a minute` (`apps/mobile/lib/background-sync-task.ts`, #1001).
- `Mobile background sync registered` with interval (same file).
- `Mobile background sync registration checked` with `decision` (register | re-register | unchanged | unregister | deferred-until-foreground), `registered`, `storedInterval`, `interval`, `appState` (same file; a headless cold start must log `deferred-until-foreground`, never a re-register). Promoted from v1.2.7 after ltguillaume confirmed the background-job fix on 1.2.7 (2026-09-03).
- Desktop `Sync backend selected; running the verification sync to activate it` (`useSyncSettings.ts`).

## v1.3.0 (add before tagging, trim in the release after)

- **`v1.3.0/project-lifecycle-sync`** — `packages/core/src/sync-normalization.ts`, when sync canonicalization clears an archived project's Focus flag or repairs cancellation/status coherence using the same rule as load. Message: `Project lifecycle normalized for sync`. Fields: `releaseCheck`, `count` (`1`). A tester syncing a legacy affected project must see this marker, then no repeated project conflict after the canonical document is saved on both sides. The marker proves normalization, not persistence or a successful full sync. No project text, identifiers, or timestamps are logged.

### Added

- **`v1.3.0/share-card-export`** — desktop and mobile `lib/share-card-export.ts`, after a local PNG is created for sharing, saving, or copying. With Diagnostics enabled, create a card from Weekly Review: the exported log must show the marker at the verified PNG/export stage. Failed mobile exports log `failureStage`, `errorType`, and an optional native `nativeCode` without the platform error message. This proves the local export path ran, not that a recipient received the image or that the QR destination is reachable. No task or project names, identifiers, reflection text, image contents, error messages, or destination file paths are logged.

- **`v1.3.0/task-editor-upgrade-sync`** — `packages/core/src/sync-merge-settings.ts`, when layout conflict resolution accepts an incoming layout over migration-generated defaults or resolves a tied/missing GTD clock. Message: `Task editor layout sync conflict resolved`. Field: `reason` (`explicit-layout` or `tied-clock`). With GTD settings sync enabled, sync existing clients whose layouts differ without editing a layout option: the receiving client must show the marker, both layouts must agree after syncing, and aligned syncs must stop emitting it. This proves merge selection, not durable upload; verify sync completion separately. No layout values, task text, identifiers, or timestamps are logged.

- **`v1.3.0/android-adaptive-window`** — mobile `hooks/use-adaptive-window.tsx`, after native window information commits a changed layout/posture. Message: `Android adaptive window updated`. Fields: `releaseCheck`, `count` (active separating/occluding folding features), `reason` (compact/expanded and flat/book/tabletop). Resize or fold the Android app with Diagnostics enabled: the exported log must show the resulting layout/posture, without repeated entries for unchanged geometry. This proves the native adaptation path ran, not draft restoration or physical Galaxy compatibility. Runtimes without the native module must not emit this success marker. No task text, identifiers, device model, or screen contents are logged. (#1192)

- **`v1.3.0/android-activity-session-recovery`** — mobile `app/_layout.tsx`, after a verified configuration replacement restores the sanitized React Navigation root through `resetRoot`, and `components/task-edit-modal.tsx`, after Android confirms a recovered task input accepted focus. Messages: `Android activity navigation restored` and `Android activity editor input restored`. Fields: `releaseCheck`, `outcome` (`restored` or `focused`), `surface` (`navigation`, `title`, or `description`). With Diagnostics enabled, leave a regular task or project flow open and trigger Activity recreation: the exported log must show the navigation marker once the replacement navigation container is ready; an editor whose title or description had focus must also show the matching focused-input marker. These prove the same-process recovery paths ran; they do not cover process death. No task text, identifiers, route parameters, device model, or screen contents are logged. (#1192)

- **`v1.3.0/sandbox-workspace`** — desktop `lib/sandbox-session.ts` and mobile `lib/sandbox-workspace.ts` emit `stage=entry-idle` after draining personal sync and local writes while new foreground sync admission is blocked. Opening must stay in the personal workspace if that check fails or times out. Desktop `main.tsx` and mobile startup then emit `workspace=sandbox` after selecting isolated in-memory storage. The tester log must show the entry marker followed by the workspace marker when opening; resetting only needs the workspace marker. Verify the visible banner, editing/reset/exit, and unchanged personal data separately. No task text, personal settings, file paths, or credentials are logged. (#1196)

- **`v1.3.0/macos-swift-runtime`** — `apps/desktop/src-tauri/src/lib.rs`, at the end of macOS native setup after registering diagnostics logging. Message: `macOS native startup reached after runtime loading`. On the affected Intel Mac, enable Diagnostics and relaunch the next build; the log must contain this marker. It proves dyld loaded the native app and setup reached this point, not that the UI or every optional native capability works. No device identifiers or task data are logged. Native CI also loads the release library, and macOS release packaging checks the executable runtime search path for both architectures.

- **`v1.3.0/shortcut-capture-single-owner`** — `apps/mobile/hooks/root-layout/use-root-layout-external-capture.ts`, after the validated shortcut delivery requests its prefilled confirmation. Message: `Shortcut capture confirmation requested`. With Diagnostics enabled, run an Action Button capture on both a cold and warm iPhone launch: this marker must appear once per delivery, the title and optional fields must be preserved, and Save with Add another off or Cancel must leave no blank capture form. A later deliberate invocation must open again. The marker proves the confirmation navigation request, not a completed save or on-device visual confirmation. No task content, URL, or identifiers are logged.

- **`v1.3.0/dropbox-callback-stream`** — desktop `sync.rs`, after an accepted Dropbox OAuth callback socket is explicitly configured for blocking IO with bounded read and write timeouts. Message: `Dropbox OAuth callback stream configured`. The tester log must show this marker during Dropbox sign-in, followed by the existing matching-state acceptance line when authorization succeeds; a stale callback must still be rejected while the valid callback remains acceptable. This marker proves socket configuration, not successful authentication. No authorization code, state, token, query, or credential content is logged.

- **`v1.3.0/calendar-push-inventory`** — desktop `desktop-calendar-push-sync.ts` and mobile `calendar-push-sync.ts`, on the existing full calendar push completion log after the shared run returns. A full run must show this marker with its existing task and failure counts. It proves the run used an invocation mapping inventory instead of per-task storage lookups; it does not claim every event or mapping write succeeded. Partial runs do not emit this marker. No task content, calendar account data, or credentials are logged.

- **`v1.3.0/watch-outbox-retry`** — `apps/mobile/lib/pending-captures.ts`, after a Watch text or audio capture marked as retried from a failed outbox save reaches task persistence, pending saves finish, and its queue item is removed. Message: `Watch outbox retry ingested`. Fields: `releaseCheck`, `kind` (`text` or `audio`), `outcome` (`created`). On Watch, a failed text outbox save must disable the separate Type control while Edit capture and Resend capture remain available. Recover using Resend capture or Retry recording; the phone log must show this marker once delivery and ingestion succeed. A failed phone flush must emit no success marker. Retry ownership before the Watch outbox save succeeds is in-memory; this marker does not claim survival of Watch process termination during that failure. No task content or identifiers are logged.

- **`v1.3.0/archive-section-preserved`** — desktop `storage.rs`, when the native reference sanitizer preserves valid section links owned by an archived project. Message: `Archived project section references preserved`, with `extra.releaseCheck` and `count`. Archive or cancel a project containing sectioned tasks, save and restart, then reactivate it: the native log must show this marker, valid links must survive, and unedited actionable children must restore. The marker proves the relationship check ran, not that the surrounding storage transaction committed. No task content or identifiers are logged.

- **`v1.3.0/archive-reference-preserved`** — `packages/core/src/store-load-migrations.ts`, when loading a normally archived project preserves its reference children. Message: `Archived project reference preserved during load migration`. Fields: `releaseCheck`, `count`. Archive a project containing reference material, then restart: the log must show the preserved count and the reference task must keep its status and completion metadata. This marker proves the preservation check ran; it does not report a new write. No task content or identifiers are logged.

- **`v1.3.0/mobile-cancel-draft`** — `apps/mobile/components/task-edit/use-task-edit-state.ts`, after Cancel writes the validated task draft as one archived outcome and pending storage saves finish. Message: `Mobile task cancellation draft saved`. Fields: `releaseCheck`, `outcome` (`cancelled`). Edit the title or Notes, add a file, and cancel the task: the log must show this marker after the save succeeds, the editor must close, and the archived task must retain the edits without creating another recurrence. A rejected write or failed flush must keep the editor open and emit no success marker. No task content or identifiers are logged.

- **`v1.3.0/widget-terminal-preserved`** — `apps/mobile/lib/pending-captures.ts`, after an Android widget completion targeting an archived or cancelled task is resolved without changing the task, pending saves finish, and the queue file is removed. Message: `Widget check-off ingested`. Fields: `releaseCheck`, `outcome` (`terminal`). Queue a widget check-off, cancel or archive that task before ingestion, then open the app: the log must show this marker and the task must retain its archived outcome without generating another recurrence. No task content or identifiers are logged.

- **`v1.3.0/watcher-property-order`** — desktop `local-data-watcher.ts`, once per controller after preparing and retaining a self-write snapshot with the shared serializer's bounded property-order reuse. Message: `Self-write snapshot prepared with bounded property ordering`. Field: `releaseCheck`. With logging enabled at launch, a local save must emit this line; an earlier save may emit it first. It proves snapshot preparation ran, not that a particular shape hit the cache, persistence completed, or latency improved. No task content, identifiers, property names, or settings values are logged.

- **`v1.3.0/storage-baseline-equality`** — desktop `storage-save-baseline.ts`, once per WebView session when two distinct snapshots compare structurally equal and skip fingerprint construction. Message: `Storage snapshot comparison skipped fingerprinting`. Field: `releaseCheck`. With logging enabled at launch, a save comparing cloned unchanged rows must emit this line; an earlier baseline/provenance/settings comparison can emit it first. It proves the positive-equality shortcut ran, not a durable commit or a measured latency. Structural mismatches still use the existing stable fingerprint path. No entity content, identifiers, or settings values are logged.

- **`v1.3.0/sqlite-snapshot-append`** — desktop Rust `storage.rs`, once per process after a normal snapshot merge commits with the append-only task path. Message: `SQLite capture snapshot retained unchanged tasks`. Fields: `releaseCheck`, `count` (retained task rows). Capture a task into an existing dataset with no simultaneous changes: a positive count proves existing task rows were retained and the transaction committed. Edits, container/settings changes, reference repairs affecting existing rows, and exact restores keep the full replacement path. This does not prove recovery JSON publication or a specific latency. No task content, IDs, paths, or credentials are logged.

- **`v1.3.0/sqlite-snapshot-statements`** — desktop Rust `storage.rs`, once per process after `persist_data_snapshot` commits using connection-cached INSERT statements. Message: `SQLite snapshot committed with cached inserts`. Fields: `releaseCheck`, `count` (canonical task count). A tester capturing or saving a full snapshot must see the line in the exported Diagnostics log; an earlier normal save may emit it first. It proves the cached-statement path committed, not completion of the recovery JSON publication or a measured latency improvement. SQL, foreign-key/FTS behavior, revision arbitration, and transaction boundaries remain unchanged. No task content, identifiers, paths, or credentials are logged.

- **`v1.3.0/task-menu-project-create`** — desktop `useTaskQuickActionMenuProps.ts`, after a project created from a task's quick-action menu is durably saved. Message: `Task menu project creation saved`. Fields: `releaseCheck`, `outcome` (`created`). Create a project from **Project…**: this line proves destination creation completed, not task reassignment; the task moves only after **Save**. Failed creation must not emit this line. No task or project text or identifiers are logged. (#1187)

- **`v1.3.0/ai-request-stop-once`** — desktop/mobile `lib/ai-config.ts`, called by the AI provider when a request terminates on timeout or cancellation without retrying. Message: `AI request stopped without retry`. Fields: `releaseCheck`, `outcome` (`timeout` | `aborted`), `provider`, `timeoutMs` (configured limit). With Diagnostics enabled, choose 120 seconds under AI → Advanced and time out one request: the log must show one `timeout` entry with `timeoutMs=120000`, not three attempts. Cancelling must show `aborted` and dispatch no further attempt. Applies to assistant/Copilot requests, not transcription or model discovery. No task text, prompts, API credentials, endpoint, or model name is logged. (#1188)

- **`v1.3.0/sync-attachment-copy-elision`** — core `sync.ts`, after full merging and reference repair, when at least one matched task/project kept its normalized winner because the attachment result was unchanged. Message: `Sync merge skipped unchanged attachment copies`. Fields: `releaseCheck`, `count` (avoided parent copies). With Diagnostics enabled, merge overlapping tasks without attachments and confirm a positive count. This proves the allocation-saving branch ran, not durable persistence or remote acknowledgement. Attachment reconciliation, conflict arbitration, cancellation and recurrence repairs still execute. No task content, identifiers, paths or credentials are logged.

- **`v1.3.0/settings-default-coload`** — desktop `SettingsView.tsx`, in the General page layout effect. Message: `[settings-open] settings-view-layout-effect`, with `page=main` and the existing trace timing fields. With Diagnostics enabled, first opening Settings must show this line with General content available in the route's initial commit, without a second page-code loading fallback. This proves a committed default page, not completion of asynchronous native configuration reads. Other sections retain `settings-page-transition`; no settings values, task content, or paths are logged.

- **`v1.3.0/settings-page-transition`** — desktop `SettingsView.tsx`, in the page layout effect after a settings destination commits. Message: `[settings-open] settings-view-layout-effect`. Fields: `releaseCheck`, `page` (fixed settings page name), plus the existing trace timing fields. With Diagnostics enabled, navigate from General to a previously unopened section: the old page should remain visible while the code loads, followed by this line for the destination. This proves a page commit, not completion of its asynchronous configuration reads. No task content, settings values, or paths are logged.

- **`v1.3.0/pomodoro-alert-delivery`** — mobile `lib/notification-service-local.ts`, when normal timer completion reaches JavaScript at or after the saved deadline and leaves the native completion alarm intact for OS delivery. Message: `Pomodoro due alarm preserved`. Fields: `releaseCheck`, `reason` (`timer-not-running` or `phase-replaced`), `outcome` (`preserved`), `count` (`1`). With Diagnostics enabled, a tester completing a session while Android delays an inexact alarm must see this line before the later audible notification. It proves Mindwtr did not cancel the due native alarm; it does not claim that the OS delivered or presented it. No task text, alarm id, timestamp, or notification content is logged.

- **`v1.3.0/sync-signature-pruning`** — core `sync.ts`, after the normal sync cycle completes full snapshot merging using signatures that filter absent/excluded fields before sorting. Message: `Full sync merge completed`. Fields: `releaseCheck`, `elapsedMs` (elapsed merge time only), `count` (merged tasks). With Diagnostics enabled, a tester syncing a real remote snapshot must see the line; the explicit empty-remote bypass must not emit it. This proves merge computation completed, not local persistence or remote acknowledgement. No task text, identifiers, paths, or credentials are logged.

- **`v1.3.0/settings-lazy-resources`** — desktop `SettingsView.tsx`, `[settings-open] settings-resource-activation`. With Diagnostics enabled, a fresh General Settings visit must show `page=main` and `integrationsLoadEnabled`, `syncLoadEnabled`, `advancedLoadEnabled` all false. Visiting Integrations, Sync/Data, or Advanced enables only that resource group; it stays enabled until Settings closes so drafts survive navigation. Credential recovery remains active. No configuration values or task content are logged.

- **`v1.3.0/startup-readiness`** — mobile `hooks/use-startup-screen-ready.tsx` and desktop `lib/startup-profiler.ts`. Message: `Startup screen ready`, with `elapsedMs` and (mobile) a fixed `route`; desktop additionally records `moduleElapsedMs`. With Diagnostics logging enabled, the line must appear only after successful canonical local hydration and the active screen's paint opportunity, never for a backup-only/error/locked screen. Mobile detailed profiling also emits `js.interactive_ready`; Android's optional native module calls `reportFullyDrawn()`. These app-side milestones are not a measurement of input acknowledgement or total native process launch. No task content is logged.

- **`v1.3.0/macos-widget-capture`** — `apps/desktop/src-tauri/src/macos_widget.rs`, when the widget's payload-free native notification reaches the host. Message: `macOS widget capture request delivered to host`, carrying `extra.releaseCheck=v1.3.0/macos-widget-capture`. A tester tapping Quick capture on the macOS widget must see this line and only the floating Quick Add panel, with no main-window reveal. Verify both a fully quit and an already-running app; Save must create one Inbox task through the normal store and Cancel must create none. This line proves request delivery, not a completed task save. No task text or identifiers are logged.
- **`v1.3.0/android-compact-widget`** — `apps/mobile/lib/widget-service.ts`, after the native refresh reports a placed Compact widget. Message: `Compact Android widgets refreshed`. Fields: `releaseCheck`, `count` (Compact widgets asked to redraw). A tester must see a positive count after opening the app with this style placed. Confirm the small flat list visually and capture from its footer while the main app is closed; the existing `Quick capture dialog item ingested` line proves the queued task reaches the store on the next app foreground. No task content or widget identifier is logged.

- **`v1.3.0/android-quick-capture-audio`** — `apps/mobile/lib/pending-captures.ts`, after a native Android recording is validated, retained for retry, or ingested into durable task storage. Fields: `releaseCheck`, `kind`, `outcome`. A tester records and saves from the widget with the main app closed, then opens Mindwtr: the log must show the audio reaches ingestion once; unavailable transcription or a failed save must retain the recording for a later attempt. Confirm permission denial, Cancel, and recording interruption on the device. No transcript, task text, path, or identifier is logged. (#1184)
- **`v1.3.0/capture-audio-auto-start-once`** — `apps/mobile/components/use-quick-capture-audio.ts`, when a visible quick-capture sheet consumes its one automatic audio-start attempt. Message: `Quick capture audio automatic start attempted`. Fields: `releaseCheck`. With Diagnostics enabled and Audio preferred, opening Quick Capture with speech-to-text unconfigured must show this line once, followed by one configuration notice; it must not repeat until a new capture opens. A manual microphone retry remains available and does not emit this automatic-attempt line. No task, task text, transcript, audio content, path, identifier, or configuration value is logged.
- **`v1.3.0/file-sync-streaming-permissions`** — `apps/desktop/src/lib/sync-attachment-backends.ts`, after a File Sync attachment generation is published or verified for reuse, or a downloaded attachment is installed. Message: `File Sync attachment transfer completed`. Fields: `releaseCheck`, `operation` (`upload` | `download`). A tester adding a file and syncing must see `operation=upload`; a peer receiving it must see `operation=download`. The line proves attachment transfer or verified reuse, not completion of the whole sync cycle. No file name, path, content, or identifier is logged. Native streaming-command refusals retain only the fixed command name in the warning.
- **`v1.3.0/reopen-project-task`** — `packages/core/src/store-tasks.ts` and `apps/cloud/src/server.ts`, only after the task-triggered project reactivation task/project/section snapshot has reached durable storage. Messages: `Task project reactivation saved` (app store) and `Cloud task reopened archived project` (Cloud REST). Fields: `releaseCheck`, `outcome` (`reactivated`), `count` (reactivated projects). A failed write, including an unchanged retry of an earlier optimistic state, emits no reactivation success line. No task content, entity id, path, namespace, token, credential, or timestamp is logged.
- **`v1.3.0/commitment-cancelled`** — `packages/core/src/store-tasks.ts` and `packages/core/src/store-projects/project-actions.ts`, after `cancelTask` or `cancelProject` has flushed the accepted cancellation to durable storage. Message: `Commitment cancellation saved`. Fields: `releaseCheck`, `kind` (`task` | `project`), `outcome` (`cancelled`), `count` (`1`). A tester cancelling a task or project must see exactly one success line after the write persists; a storage failure returns an action error and emits no success line. No title, notes, entity id, timestamp, attachment data, or cancellation reason is logged.
- **`v1.3.0/android-capture-intent`** — `apps/mobile/lib/pending-captures.ts`, after an Android automation capture reaches durable task storage and leaves the pending queue. Message: `Android automation capture ingested`. Fields: `releaseCheck`. The tester must see one line for each accepted note after opening Mindwtr; two notes sent before opening must produce two Inbox tasks and two lines. No task text, token, or identifier is logged. (#1149)
- **`v1.3.0/widget-focus-today`** — `apps/mobile/lib/widget-service.ts`, after Android accepts the curated widget payload. Message: `Android widget Focus and Today payload published`. Fields: `releaseCheck`, `focusItems`, `todayItems`, `totalItems`. The tester's log must show the published counts for Today's Focus and Today, within the shared 20-task Android limit. Other Focus sections are omitted from the default widget; explicit chooser lists remain available. No task content or identifiers are logged. (#1173)
- **`v1.3.0/ios-focus-widget`** — `apps/mobile/lib/widget-service.ts`, after all iOS family payloads are written and timelines requested. Message: `iOS Focus widget payload published`. Fields: `releaseCheck`, `focusItems`, `todayItems`, `totalItems` (default-family budget, at most 12). The tester must see this line after changing Focus filters or tasks, then visually confirm Today's Focus and Today, selected lists, and the appropriate widget size. Publication does not prove WidgetKit rendered the timeline. No task content or identifiers are logged. (#1173)
- **`v1.3.0/ios-widget-checkoff`** — `apps/mobile/lib/ios-widget-completions.ts`, after a claimed App Group completion is resolved through the normal store, pending saves flush, a fresh widget payload is published, and the action is acknowledged. Message: `iOS widget completion ingested`. Fields: `releaseCheck`, `outcome` (`completed`, `already-done`, `terminal`, `missing`, or `stale`). Check off from an iOS 17+ Home Screen widget, then open Mindwtr: the line must appear and the task must be done exactly once, including recurrence. Undo before the app claims an action must produce no completion. A changed or reopened task must produce `stale` without changing it. A failed save must retain the action for retry. No task content or identifiers are logged. (#1173)
- **`v1.3.0/cloud-focus-write-parity`** — `apps/cloud/src/server.ts`, after a REST task creation requests Focus or a normalized PATCH changes Focus from false to true. Message: `Cloud task Focus write policy applied`. Fields: `releaseCheck`, `operation` (`create` | `patch`), `outcome` (`focused` | `refused-ineligible` | `refused-limit`), `count` (active Focus tasks after an accepted write, or at refusal). A refused create still returns the captured task with its requested status and no star; a refused PATCH returns `Focus limit of <limit> reached`. No task content, id, namespace, or token is logged.
- **`v1.3.0/webdav-presence-proof`** — `apps/desktop/src/lib/sync-attachment-backends.ts` and `apps/mobile/lib/attachment-sync-backends/webdav.ts`, after the shared remote attachment presence proof finishes. Message: `WebDAV attachment presence proof finished`. Fields: `releaseCheck`, `checked`, `cleared`, `complete`. A tester recovering from a temporary WebDAV 401, 500, or network failure must first see `complete=false`, then a later recovered cycle with `complete=true`; only the complete cycle may suppress another proof for 24 hours. `cleared` counts definitive missing objects queued for same-cycle repair. No URL, credential, path, title, task text, or attachment identifier is logged.
- **`v1.3.0/webdav-activation-batches`** — `packages/core/src/sync-run.ts`, after an activation probe proves every required attachment across more than one explicitly deferred transfer batch and before the document write. Message: `Sync activation proved attachments across transfer batches`. Fields: `releaseCheck`, `backend`, `total` (required attachments), `batches` (bounded transfer calls). A tester activating WebDAV with more than ten local or candidate files must see this line and then confirm the subsequent sync succeeds. A failed attachment proof emits no such line; the marker proves attachment completion, not the later document write or configuration commit. No file name, path, title, task text, URL, credential, or attachment identifier is logged. (#1186)
- **`v1.3.0/watch-audio-ready`** — `apps/mobile/lib/pending-captures.ts`, after the JS queue drain accepts a Watch audio path and before transcription starts. Message: `Watch audio ready for transcription`. Fields: `releaseCheck`, `outcome` (`validated`). Reaching this line proves the native receiver published a validated WAV and its queue item; a tester's successful recording must show it followed by the existing `Watch capture ingested` line, while a later transcription failure leaves the WAV and queue item for retry. No recording content, path, or identifier is logged. (#1175)
- **`v1.3.0/watch-capture`** — `apps/mobile/lib/pending-captures.ts`, after a Watch text or transcribed audio capture reaches durable task storage and leaves the pending queue. Fields: `releaseCheck`, `kind`, `outcome`. The tester must see `Watch capture ingested` with `outcome=created` for each ingested Watch capture. Audio transcription failure/unavailability logs that outcome and retains the queue and recording for retry. No title, transcript, audio path, or task identifier is logged. (#1175)
- **`v1.3.0/watch-command`** — `apps/mobile/lib/pending-captures.ts`, after a Watch completion, defer, or Pomodoro command is applied or resolved as a no-op and removed from the queue. Message: `Watch command ingested`. Fields: `releaseCheck`, `kind`, `action` (Pomodoro only), `outcome`. A tester's log distinguishes an applied command from a stale or terminal-task no-op after reconnecting. (#1175)
- **`v1.3.0/watch-numeric-bridge`** — `apps/mobile/hooks/root-layout/use-root-layout-watch.ts`, after the awaited native bridge accepts the Focus/timer application context. Message: `Watch snapshot published`. Fields: `releaseCheck`, `outcome` (`published`), `focusCount`, `timerPhase`, `timerRunning`. This proves Foundation numeric values passed the native validator and the iPhone published the snapshot. A paired-device check must also show `Watch capture ingested` for an incoming text capture to exercise the reverse transport bridge. No task content or identifier is logged. (#1175)
- **`v1.3.0/watch-snapshot`** — `apps/mobile/hooks/root-layout/use-root-layout-watch.ts`, when Watch connectivity activation or application-context publication fails. Messages: `Watch connectivity activation failed` or `Watch snapshot publication failed`. Fields: `releaseCheck`, `outcome` (`activation-failed` | `failed`). These content-free warnings distinguish activation from publication failure; successful native acceptance uses `v1.3.0/watch-numeric-bridge`. (#1175)
- **`v1.3.0/mobile-file-activation-absent-blob`** — `apps/mobile/lib/attachment-sync-backends/file.ts`, in the activation-probe branch of `onDownload`, when the sync folder does not hold a keyed attachment's file. Message: `File Sync activation left an attachment the folder does not hold yet`. Fields: `releaseCheck`. A phone joining a Syncthing folder whose `attachments/` lacks a file must log this once per such record followed by core's `Sync folder activation accepted attachments the folder does not hold yet`; before the fix the phone cleared the key and refused with "the file could not be fetched from the new sync location". (feedback romantashit 2026-09-06)
- **`v1.3.0/cloudkit-zone-recreated`** — `apps/mobile/lib/cloudkit-sync.ts`, in `recreateCloudKitZone`, after the native module reported `ERR_CLOUDKIT_ZONE_GONE` (CloudKit `userDeletedZone` or `zoneNotFound`) and `ensureZone` succeeded again. Message: `CloudKit zone was deleted or cleared; recreated it and reset the change token`. Fields: `releaseCheck`. A tester who cleared Mindwtr's iCloud data must see this line once, followed by a normal `CloudKit write complete`; before the fix every cycle failed with "zone was cleared by user" forever. (feedback 5d1163be)
- **`v1.3.0/system-capture-return`** — `apps/mobile/app/capture-modal.tsx`, in `finishCapture`, only when the route carries `origin=system` (set by `+native-intent.ts` for the widget, Quick Settings tile, app shortcut and capture notification) and `returnToPreviousApp` sent the Android activity to the back. Message: `Quick capture opened from a system entry point returned to the previous screen`. Fields: `releaseCheck`. A tester's log must show this line once per widget capture that ended on the home screen; a capture from the in-app + button never logs it. (#1169)
- **`v1.3.0/android-native-widget`** — `apps/mobile/lib/widget-service.ts`, in `updateAndroidWidgetsFromData`, after the payload JSON was handed to `modules/android-widget` and every placed widget was asked to redraw. Message: `Android widget payload published`. Fields: `releaseCheck`, `items` (how many task rows the payload carries, at most 20). No task title is logged. A tester whose widget shows stale or empty rows must have this line after each data change; the widget is now drawn by Android itself from that stored payload, so a line with `items` above zero next to an empty widget means the launcher did not redraw, not that the app did not publish.
- **`v1.3.0/android-widget-provider-compat`** — `apps/mobile/lib/widget-service.ts`, in `updateAndroidWidgetsFromData`, after the native module redraws at least one Tasks widget still bound to the provider component shipped in 1.2.7. Message: `Legacy Android Tasks widgets refreshed`. Fields: `releaseCheck`, `legacyWidgetCount` (number of placed widgets using the historical provider). A tester who upgrades with an old widget already placed must see this line with a count above zero and the widget must continue to update; no task content or widget identifier is logged.
- **`v1.3.0/android-quick-capture-dialog`** — `apps/mobile/lib/pending-captures.ts`, in `ingestPendingCaptures`, after a queued item whose `source` is `android-quick-capture` (written by `QuickCaptureActivity` in `modules/android-widget`) was added to the store and its file deleted. Message: `Quick capture dialog item ingested`. Fields: `releaseCheck`. A tester who saved a task from the widget's "+", the Quick Settings tile, the app shortcut or the capture notification must see this line on the next app start or foreground, once per saved item; a Shortcut item from iOS never logs it. No line while `files/pending-captures/` still holds the item means the app never drained the queue, not that the dialog failed to write. (#1169)
- **`v1.3.0/cloud-capture-audio-retained`** — `apps/cloud/src/server-capture.ts`, in failed capture cleanup when the canonical document already references the generated audio, cannot be read, or storage authority cannot be re-established. Message: `Capture audio retained after failed document write`. Fields: `releaseCheck`, `retentionReason` (`referenced` | `unreadable` | `storage-authority`). A self-hoster who receives a capture error must see this line when the server deliberately keeps the recording rather than risk deleting bytes a published or uncertain task document may reference. No transcription, audio content, token, path, namespace, attachment id, or cloud key is logged. Cloud server logs bypass core's sanitizer, so `retentionReason` is deliberately excluded from `release-diagnostics-fields.test.ts`, like `tokenScope` and the other cloud-only fields.
- **`v1.3.0/sqlite-final-tombstone-expiry`** — `packages/core/src/sqlite-adapter.ts`, after an empty snapshot commits the physical deletion of the final retained entity tombstones. Message: `SQLite final tombstone expiry persisted`. Fields: `releaseCheck`, `count`, `outcome` (`pruned`). This proves the adapter verified that every persisted entity row was an unchanged, observed tombstone beyond the default retention window and committed the cleanup. No entity content or identifier is logged.
- **`v1.3.0/cloud-capture-token`** — `apps/cloud/src/server-capture-tokens.ts`, in `createCaptureToken`, after the token file was durably written. Message: `Capture token created`. Fields: `releaseCheck`, `tokenScope` (`capture`). No id, label, digest or token is logged. The existing `Capture webhook request accepted` line (v1.2.8) now also carries `tokenScope` (`full` | `capture`), so a self-hoster can confirm which kind of token a device posted with. Cloud server log lines are written as-is by `server-config.ts` (they do not pass through core's log sanitizer, whose substring rule would redact `tokenScope`), so this field is deliberately not in `release-diagnostics-fields.test.ts`, like the other cloud fields. (#1178)
- **`v1.3.0/android-widget-checkoff`** — `apps/mobile/lib/pending-captures.ts`, in `applyPendingCompletion`, once per `kind: complete` queue item the app drains (written by the widget's ring tap after its undo window). Message: `Widget check-off ingested`. Fields: `releaseCheck`, `outcome` (`completed` = the store marked the task done, `already-done` and `missing` = no-op, file removed). A tester who checked a row off on the widget must see one line on the next app start or foreground; `outcome=completed` with the task still open afterwards would point at the store, `missing` at a stale widget payload. (#1173)

- **`v1.3.0/mcp-existing-network-link-preserved`** — `apps/mcp-server/src/service.ts` and `apps/mcp-server/src/cloud-service.ts`, after a task or project update durably preserves one or more existing network-share links through the locked local write or Cloud ETag/If-Match retry path. Message: `MCP attachment link replacement committed`. Fields: `releaseCheck`, `backend` (`local` | `cloud`), `entity` (`task` | `project`), `count` (preserved existing network-share links). A tester editing the title of an existing network-share link through MCP must see this line; its absence with an error means the guarded write was not acknowledged. No entity id, title, URI, token, credentials, or attachment metadata is logged. New and changed network-share references remain rejected. (#1154)

## v1.2.8 (add before tagging, trim in the release after)

Field names are checked against the log sanitizer by `packages/core/src/release-diagnostics-fields.test.ts`. Add every new field name to that test's list.

### Added

- **`v1.2.8/desktop-reminder-fired`** — `apps/desktop/src/lib/notification-service.tsx`, in `checkDueAndNotify` (`logReminderFired`), at all three fire sites: due-time repeats, task reminders and project review reminders. Message: `Desktop reminder fired`. Fields: `kind` (`due-repeat` | `task` | `project`), `entity` (`task` | `project`), `fireAt` (ISO occurrence time), `appState` (`focused` | `hidden`). No task title or body is ever logged. Tester's log: a user who sees no toast now has one line proving the scheduler fired, which separates a scheduling bug from a delivery bug (#1146).
- **`v1.2.8/file-activation-absent-blobs`** — `packages/core/src/sync-run.ts`, in `prepareRemoteWriteData` after `assertActivationAttachmentsProven`. Message: `Sync folder activation accepted attachments the folder does not hold yet`. Fields: `backend` (`file`), `deferred` (count), `ids` (first five attachment ids). Tester's log: a fresh desktop joining a Syncthing or mounted folder whose `attachments/` has not arrived now shows this line and the switch completes, where 1.2.7 logged `Sync failed … Candidate attachment proof failed for <id>` and left the previous sync settings active; the ids listed must later download on a normal cycle (in-app feedback, 2026-09-04).
- **`v1.2.8/desktop-notification-path`** — same file, in `sendNotification` (`logNotificationSent` / `logNotificationFailed`). Messages: `Desktop notification sent` and `Desktop notification send failed`. Fields: `path` (`flatpak` | `windows-packaged` | `plugin` | `web`) and, on the failure line, `error`. Tester's log: on a Microsoft Store install the line must read `path=windows-packaged`; a Store install that still falls through to `plugin` means the packaged command rejected, and the warning names the HRESULT it rejected with (#1146).
- **`v1.2.8/attachment-only-task-replace`** — `packages/core/src/store-settings.ts`, in `fetchData`, once per store load (never per task) when `reconcileEntityCollection`'s new `attachmentOnlyReplacedCount` is greater than zero. Message: `Sync store reconcile replaced tasks for an attachment-only change`. Fields: `entity` (`task`), `count` (how many tasks this load replaced for attachments alone). No task title or id is logged. Tester's log: on a device where a peer just changed or removed an attachment, this line proves the #1136 fix (e19270e0c) actually replaced the in-memory task instead of keeping the stale cached one, which is what caused the every-cycle resync #1136 reported (#1136, awaiting confirmation; #1156 may be a duplicate).
- **`v1.2.8/android-http-connect-timeout`** — `apps/mobile/hooks/root-layout/use-root-layout-startup.ts`, right after `App started`, Android only. Message: `Android HTTP connect timeout installed`. Field: `connectTimeoutMs` (`10000` when `SyncHttpClientPackage` in `apps/mobile/modules/sync-file-lock` bounded React Native's OkHttp client at process start; `0` means the listener did not run and the fix is inert). Tester's log: a phone whose server publishes an IPv6 address that drops connections showed `Sync failed … Cloud request timed out` exactly 30 s after `read-remote` in 1.2.7; with `connectTimeoutMs=10000` the read must now succeed over IPv4 within about ten seconds (#1150).
- **`v1.2.8/cloud-capture-accepted`** — `apps/cloud/src/server-capture.ts`, in `handleCaptureRequest`, just before the 201 response. Message: `Capture webhook request accepted`. Fields: `hasAudio` (`true`|`false`), `bodyKind` (`multipart`|`json`|`text`), `bytes` (request body size). No transcription text or token is logged. Tester's log: a self-hoster whose capture script or watch posts to the webhook now has a server-side line proving the request arrived, even when nothing appears in the app (03e9cb9eb added the route without this line).
- **`v1.2.8/fence-mutation-horizon`** — `packages/core/src/sync-remote-fence.ts`, in `assertHeld`, after a successful renewal that leaves enough safe authority for the requested provider mutation. The lease widens its advertised heartbeat interval when needed for the requested horizon without slowing its actual heartbeat timer, and never shrinks that advertisement later. Message: `Remote sync mutation fence renewed for mutation horizon`. Fields: `horizonMs` (requested protected request time), `remainingMs` (conservative time at response return before expiry or heartbeat abandonment, whichever comes first, after deducting authority-read transit). No owner or lease id is logged. Tester's log: this line proves a client extended both the expiry and heartbeat reclamation window before a long remote mutation; its absence before a fence error means the provider could not confirm enough safe time.
- **`v1.2.8/section-conversion-canonical`** — `packages/core/src/store-tasks.ts`, in `convertTaskToSection`, after the task, section and checklist children are published in one store mutation. Message: `Task converted to section with canonical child tasks`. Field: `count` (created child tasks). No task or section title or id is logged. Tester's log: this line proves conversion created every open and completed checklist child with the canonical false focus and reminder flags, avoiding a follow-up sync normalization rewrite.
- **`v1.2.8/mcp-attachment-link-guard`** — `apps/mcp-server/src/service.ts` and `apps/mcp-server/src/cloud-service.ts`, after a guarded task or project link replacement has durably completed. Message: `MCP attachment link replacement committed`. Fields: `backend` (`local` | `cloud`), `entity` (`task` | `project`). No entity id, title, URI, token, or attachment metadata is logged. Tester's log: this line proves the MCP update completed through the locked local patch factory or the Cloud ETag/If-Match retry path; its absence with an error means no guarded replacement was acknowledged.
- **`v1.2.8/archive-metadata-canonical`** — `apps/mobile/lib/storage-adapter.ts`, in `mobileStorage.getData`, after a successful SQLite read. Message: `SQLite project archive metadata loaded in canonical form`. Field: `count` (task and section rows processed by the canonical SQLite codecs). No task or section title or id is logged. Tester's log: this line proves the mobile app loaded the SQLite snapshot through the canonical archive-metadata path; after the first repaired sync, subsequent idle cycles must stop rewriting local or remote content (#1156).

### Existing (do not add a second line)

- **stale fence after abort** (0bf5784c0) — the existing `Sync follow-up scheduled` line now carries the delay that actually applies; a tester's log must no longer show `Remote sync location is reserved; retrying after the lease lapses` counting down on every device after one phone left the app mid-cycle.
- **#1151 activation refusal names the file** (61fcfb427) — the existing `Sync failed` line carries the refusal text, which now names the attachment file, its task or project, and the cause.
- **#1001 canonical reads and the inert backstop** (2c65cb62f, 44f6de358, 9f62fbb0a) — the existing `Sync local reconcile` line with `reconcile: 'aligned-skip'` proves a local-only upload skipped the merge; a desktop log must no longer show a canonical-read failure after an MCP write.
- **#1001 Android attachment throttle while timers are paused** (c9541342e) — the existing `Mobile background sync finished` line under General carries the run duration; a background run that uploads two or more attachments must finish inside the job allowance.
- **iCloud Atomic failure** (b5200e090, bb09c2463) — the CloudKit sync error shown on the Sync screen and written to the log now names the record and reason CloudKit actually rejected instead of a bystander's `Atomic failure`.

## v1.2.7 (carried forward, unconfirmed; keep the original tags and trim once each fix is confirmed)

- **`v1.2.7/sync-settings-activation-mobile`** — `apps/mobile/components/settings/use-sync-settings-transport-actions.ts` (in `handleSync`, guarded on an explicit `options.backend`, which only an activation passes). Message: `Sync backend selected; running the verification sync to activate it`. Fields: `backend`, `cloudProvider`. Tester's log: one line the moment a complete backend is chosen or saved, before any manual Sync tap. Desktop's equivalent is the existing line under **General**.

- **`v1.2.7/sync-status-published`** — `apps/mobile/lib/sync-service.ts`, in `finalizeSuccess`. Message: `Sync status published to the store`. Fields: `backend`, `statusPublished` (`wrote-local` or `unchanged`), `lastSyncAt`, `lastSyncStatus`. Tester's log: one line per successful cycle whose `lastSyncAt` advances, proving the Sync screen's "Last sync" is no longer frozen.

- **`v1.2.7/dropbox-signin-detached`** — `apps/desktop/src-tauri/src/sync.rs` (Rust `log::info!`/`log::warn!`, target `sync`). Three phases: `phase=opened` after `open::that_detached` returns, `phase=callback-state-matched` when the real callback is accepted, `phase=callback-state-mismatch` when a stale tab replays an old state and is ignored. Tester's log: `opened` must appear immediately, not after the browser closes, and a completed connect ends in `callback-state-matched`.

- **`v1.2.7/incoming-link-redelivery`** — `apps/mobile/hooks/use-incoming-url.ts`. Message: `Incoming link delivered`. Fields: `scheme`, `host` (nothing further: a capture link carries the task title in its path and query), `delivery` (counter), `deduped`. Tester's log: pressing the same shortcut twice writes two lines with `deduped=false` and rising `delivery`; only an immediate launch-URL echo shows `deduped=true`.

- **`v1.2.7/fence-artifact`** — `apps/mobile/lib/sync-service.ts`, in `acquireWebdavRemoteMutationFence`. Message: `WebDAV sync fence artifact resolved`. Field: `artifact` (basename only). Tester's log: `artifact` must read `.mindwtr-sync-fence-v1.json` and must NEVER read `data.json` (#1132).

- **`v1.2.7/calendar-spanning-events`** — `apps/mobile/lib/external-calendar.ts`, at the end of `fetchSystemCalendarEvents`. Message: `Device calendar events loaded for the window`. Fields: `platform`, `total`, `multiDay`, `allDay`, `spanning`. Tester's log: on Android a month containing a multi-day event must report `spanning` greater than zero, which is exactly what the old containment query dropped.

- **`v1.2.7/cycle-unchanged-skip`** — `packages/core/src/sync-run.ts`, `logUnchangedCycleSkips`, called from both unchanged returns. Message: `Sync cycle changed nothing; passes skipped`. Fields: `backend`, `check` (`fast` or `read`), `skipped` (comma list). Tester's log: an idle device shows this after every `Sync fast check found no changes`, with `local-persist,store-refresh,attachments,remote-fence`. The field is `skipped`, NOT `skippedPasses`, which the sanitizer redacts.

- **`v1.2.7/daily-attachment-presence`** — `apps/desktop/src/lib/sync-service.ts`, in `hasAttachmentSyncWork`. Message: `Attachment presence re-verification checked`. Fields: `presenceDue`, `hasScope`. Tester's log: `presenceDue=false` on every cycle within a day of the last full pass, and `presenceDue=true` once a day or after the sync location changes.

- **`v1.2.7/android-attachment-link-fallback`** — `apps/mobile/lib/attachment-file-installer.ts`, after the native installer reports that Android refused the hard-link publication and used the exclusive-copy fallback. Message: `Android attachment generation published with exclusive-copy fallback`. Fields: `publication` (`exclusive-copy`). Tester's log: one line with `publication=exclusive-copy` proves the #1139 fallback succeeded instead of relying on the absence of the old `Could not publish attachment generation (EXDEV)` failure.
